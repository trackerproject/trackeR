read_directory_shiny <- function(input,
                                output,
                                session,
                                ...) {
    read_directory(..., shiny = TRUE)
}


